import socket

SERVER_IP = 'localhost'
PORT = 12345

def consumer():
    client_socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    client_socket.connect((SERVER_IP, PORT))

    while True:
        # Implement your logic to consume data items here
        pass

consumer()
